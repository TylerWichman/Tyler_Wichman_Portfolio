# Full pipeline dependencies for generate_snapshot.py (used only by the GitHub Actions
# cron job -- the deployed Streamlit app itself never installs this, see requirements.txt).
# Versions pinned to what was actually verified working throughout this project's build.
pandas==3.0.5
numpy==2.5.2
scikit-learn==1.9.0
scipy==1.18.0
requests==2.34.2
nflreadpy==0.1.5
python-dotenv==1.2.2
pyarrow
